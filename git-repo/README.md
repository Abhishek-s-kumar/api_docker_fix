# Rules repository placeholder
# In production, this directory is populated by:
#   1. Git clone of your rules repository (configured via GIT_REMOTE_URL)
#   2. OR manual placement of rule XML files

# Directory structure expected:
#   git-repo/
#   ├── rules/          ← Wazuh rule XML files
#   ├── decoders/       ← Decoder XML files
#   └── lists/          ← CDB list files

# Example local rule (for testing):
# git-repo/rules/local_rules.xml
